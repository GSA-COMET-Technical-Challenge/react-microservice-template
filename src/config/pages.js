import react from "react"
import SearchPage from "../pages/Search"

const config = [
    {
        name: 'search',
        path: '/search',
        component: <SearchPage />,
        navbar: true
    }
]

export default config