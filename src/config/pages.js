import React from "react"
import Search from "../pages/Search"

const config = [
    {
        name: 'Search',
        path: '/search',
        component: Search,
        navbar: true
    }
]

export default config